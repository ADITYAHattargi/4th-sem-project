{
  "name": "skillbloom-static-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "SkillBloom static HTML frontend with Express backend",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=18"
  }
}
